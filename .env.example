# --- OpenAI ---
OPENAI_API_KEY=sk-...

# --- Telegram ---
TELEGRAM_BOT_TOKEN=123456789:ABC...

# --- Google Calendar ---
# Path to the OAuth credentials JSON downloaded from Google Cloud Console
GOOGLE_CREDENTIALS_FILE=credentials.json
# Path where the app stores the OAuth token after first login (auto-created)
GOOGLE_TOKEN_FILE=token.json

# --- App settings ---
TIMEZONE=Europe/Warsaw
OPENAI_MODEL=gpt-4o

# --- User profile (required) ---
# Multiline value — wrap in double quotes. Updated automatically by the bot when you share new info.
USER_PROFILE="Name: ...
Age: ...
..."
